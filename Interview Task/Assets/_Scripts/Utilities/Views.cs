namespace LittleSimWorld
{
    public enum Views
    {
        Front = 0,
        Back = 2,
        Left = 1,
        Right = 3

       //yes, I'm setting up values so is to easier to convert it to Directions (enum) :D
    }
}